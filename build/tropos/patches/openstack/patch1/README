Refer https://wso2.org/jira/browse/OZONE-22
and https://wso2.org/jira/browse/OZONE-20
In the Openstack nova controller comment the following lines in the  file /usr/lib/python2.7/dist-packages/nova/virt/libvirt/connection.py

        #if FLAGS.libvirt_type == 'lxc':
        #    disk.destroy_container(self.container)
        #if os.path.exists(target):
        #    shutil.rmtree(target, ignore_errors=True)


