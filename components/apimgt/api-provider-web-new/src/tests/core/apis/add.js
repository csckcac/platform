var test0 = function() {
    var name = "addAPI";
    var utils = require("/tests/utils.js");
    var add = require("/core/apis/add.js");
    //var result = add[name](5);
    //if(result.error) {
   //     utils.failure(name, result);
   //     return;
   // }
  //  utils.success(name, result);
};