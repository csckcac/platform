<%
var generateAPIKey = function (api, context, application, username) {
    var key,
            log = new Log(),
            store = jagg.module("greg").getAPIStoreObj();

    try {
        key = store.getKey(api.provider, api.name, api.version, context, application, username);
        if (log.isDebugEnabled()) {
            log.debug("generateAPIKey");
        }
        return {
            error:false,
            key:key
        };
    } catch (e) {
        log.error(e.message);
        return {
            error:e,
            key:null
        };
    }
};
%>
