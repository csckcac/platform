<%
include("/jagg/jagg.jag");
var site = require("/site/conf/site.json");

(function () {
    var apis, result, obj,
            tag = request.getParameter("tag"),
            query = request.getParameter("query");
    if (tag) {
        obj = require("/modules/api/tagged.js");
        result = obj.getAPIsByTag(tag);
        apis = result.apis;
    } else if(query) {
        obj = require("/modules/search/apis.js");
        result = obj.searchAPIs(query);
        apis = result.apis;
    } else {
        obj = require("/modules/api/published.js");
        result = obj.getAllPublishedAPIs();
        apis = result.apis;
    }


    jagg.render({
        "name":"page/base",
        "inputs":{
            "title":"API Store Listing",
            "body":[
                {
                    "name":"layout/extended",
                    "inputs":{
                        "top":null,
                        "left":null,
                        "middle":{
                            "name":"api/listing",
                            "inputs":{
                                "apis":apis
                            }
                        },
                        "right":null
                    }
                }
            ]
        }
    });
}());
%>