var initialize = function(data, apis) {

    //var site = require("/site/conf/site.json");
    //var listing = require("/core/listing/listing.js");

    //var result = listing.getAllPublishedAPIs();

    //addTemplateData(data, "published-apis", "apis", apis);
};