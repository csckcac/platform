var failure = function(method, result) {
    log("FAILURE : " + method);
    log("=====================================================================================");
};

var success = function(method, result) {
    log("SUCCESS : " + method);
};