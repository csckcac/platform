<APIManager>
    <!--
        Database configuration used by API provider and API store web applications.
        When these two applications are deployed separately, they should have
        separate database configurations pointing to the same physical database.
    -->
    <Database>
        <!--
            JDBC connection string for the database.
        -->
        <URL>jdbc:h2:repository/database/WSO2AM_DB</URL>

        <!--
            JDBC username for the database.
        -->
        <Username>wso2carbon</Username>

        <!--
            JDBC password for the database.
        -->
        <Password>wso2carbon</Password>

        <!--
            JDBC driver for the database.
        -->
        <Driver>org.h2.Driver</Driver>
    </Database>

    <!--
        Credentials for the API gateway admin server. This configuration
        is mainly used by the API provider to connect to the API gateway and
        create/update published API configurations.
    -->
    <APIGatewayAdmin>
        <!--
            Server URL of the API gateway.
        -->
        <ServerURL>https://localhost:9443/services/</ServerURL>

        <!--
            Admin username for the API gateway.
        -->
        <AdminUsername>admin</AdminUsername>

        <!--
            Admin password for the API gateway.
        -->
        <AdminPassword>admin</AdminPassword>

        <!--
            Hostname of the API gateway.
        -->
        <AdminHost>localhost</AdminHost>
    </APIGatewayAdmin>

    <!--
        API usage tracker configuration used by the BAM data publisher in API
        gateway.
    -->
    <APIUsageTracking>
        <!--
            Enable/Disable the API usage tracker.
        -->
        <Enabled>false</Enabled>

        <!--
            Thrift port of the remote BAM server.
        -->
        <ThriftPort>7612</ThriftPort>

        <!--
            Server URL of the remote BAM server used to collect statistics. Must
            be specified in protocol://hostname:port/ format.
        -->
        <BAMServerURL>https://localhost:9444/</BAMServerURL>

        <!--
            Administrator username to login to the remote BAM server.
        -->
        <BAMUsername>admin</BAMUsername>

        <!--
            Administrator password to login to the remote BAM server.
        -->
        <BAMPassword>admin</BAMPassword>
    </APIUsageTracking>

    <!--
        Configuration used by the API gateway to authenticate API consumers (subscribers).
        The provided credentials are used to connect to the authentication admin services
        of the API gateway and authenticate subscribers based on the API key values
        they provide.
    -->
    <APIConsumerAuthentication>
        <!--
            Admin username for API gateway.
        -->
        <AdminUsername>admin</AdminUsername>

        <!--
            Admin password for API gateway.
        -->
        <AdminPassword>admin</AdminPassword>
    </APIConsumerAuthentication>
</APIManager>

