<%
    include('includes/template_header.jag');

    var result = request.getParameter("result");
    var taskKey = request.getParameter("taskKey");
    var projectKey = request.getParameter("projectKey");
%>
<div id="middle">
    <h3>Pending Approvals</h3>
    <div id="workArea">
        <%
            if (result == "approved") {
                print("task approved " + taskKey + " for project key " + projectKey);
            } else {
                print("task rejected" + taskKey + " for project key " + projectKey);
            }
        %>
    </div>
</div>
<%
    include('includes/template_footer.jag');
%>
